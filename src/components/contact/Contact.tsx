import styled from "styled-components";

const Contact = () => {
  return <StyledContact id="contact">Contact</StyledContact>;
};

export default Contact;

const StyledContact = styled.section`
  background-color: inherit;
  z-index: 30;
`;
